# Copilot Instructions

## Project Guidelines
- Keep file-surgery style logic encapsulated in helper methods near the bottom of `TestHelper` instead of inline in tests.
- Ensure test logs narrate what the test is proving (intent/phase markers) using concise, meaningful log lines, along with helper side-effect logging; avoid adding verbose/noisy logs.